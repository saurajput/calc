namespace Calculation
{
    public class Calc
    {
        public int Add(int x, int y)
        {
            var op = x + y;
            Console.WriteLine("the sum we got is " + op);
            return op;
        }
    }
}
